# HANDOFF.md — プロジェクト引継ぎドキュメント（AI エージェント用）

このファイルは「今このプロジェクトがどこまで進んでいて、次に何を確認すべきか」を
**1 ファイルで把握できる** ようにするための状況スナップショットです。
`docs/PS1_REDESIGN.md` の「新セッション向け冒頭指示 + 進捗ログ」の運用パターンを
プロジェクト全体に拡張したものにあたります。

---

## ⚠️ 新しいセッションを開始した Claude へ（読む順序）

1. **本ファイルを最後まで読む**（§1〜§6）。他ドキュメントを横断しなくても現在地が分かるようにしてある。
2. 次に何をやるかは列挙していない（陳腐化するため）。**`/next-task` skill** で `ISSUES.md` と `roadmap/PROJECT.md` を突き合わせた候補を出してもらう。
3. 個別領域を深掘りする必要が出たら §6 の地図から該当ドキュメントへ。特に **PS1 表現（Phase 3）に触るなら `docs/PS1_REDESIGN.md` は必読**（禁忌・失敗履歴の単一の真実の源）。
4. 意味のある区切りの作業が終わったら **§5 セッションログに 1 行追記**すること。次のセッションのために。

---

## §1 プロジェクト概要

React 19 + `@react-three/fiber` + Three.js で作られた、一人称視点の PS1 風 3D ダンジョン
探索ゲーム（迷路生成・アイテム収集・敵/罠・スコアランキング）。個人開発で、
ゲームとしての完成に加えて **開発者本人の AWS / IaC / SRE スキル習得の実地媒体**
という二重の目的を持つ（詳細は `roadmap/CAREER.md`）。

---

## §2 Phase 進捗マップ

詳細なチェックボックスと Milestone 単位の説明は `roadmap/PROJECT.md` を参照。ここでは
Phase 単位の状態だけをまとめる。

| Phase | 内容 | 状態 |
|---|---|---|
| 0 | 基盤整備 | 完了（lint/build のグリーン化のみ Node バージョン更新待ち → §4） |
| 1 | ゲームプレイ拡張（スコア永続化・難易度・サウンド・ミニマップ） | 完了 |
| 2 | 世界観（ドア/鍵・トラップ・敵 AI） | 完了 |
| 3 | PS1 表現の深化（創作トラック） | **進行中**。詳細は §3・`PS1_REDESIGN.md` |
| 4 | 公開とサーバーレスバックエンド（スキル習得トラック・最優先） | 一部実装。詳細は §3 |
| 5 | インフラのコード化（IaC） | ツール選定のみ完了（Terraform、`infra/IAC_CHOICE.md`）。コード化未着手 |
| 6 | SRE・可観測性 | 未着手（Phase 4/5 完了後） |
| 7 | 品質・運用 | 一部（Vitest で `MapGenerator` のみカバー、`useFrame` alloc 削減済み） |

---

## §3 今アクティブな作業とその状態

### Phase 3: PS1 表現（`docs/PS1_REDESIGN.md` が単一の真実の源）

6 ステップ計画のうち:

- **Step 1（内部レンダー解像度を PS1 域に固定）: 完了・ユーザ目視確認 OK**。`develop` へマージ済み（`b9f68b4`）。ユーザ評価「今までで一番いい状態のグラフィック」。
- **Step 6 の一部（fog 値調整）を先行実施済み**: `(8, 32)` で運用中・暫定 OK。
- **Step 2〜5（Floor/Ceiling の subdivide、頂点スナップ再導入、アフィンテクスチャ再導入、ディザ再導入）: 未着手（待機中）**。
- 頂点スナップ単独・アフィンテクスチャ単独は過去に試行してユーザ却下済み（禁忌）。再挑戦は Step 2 のテッセレーションを土台にした上で、という条件付き。**新たに PS1 見た目の提案をする前に必ず `PS1_REDESIGN.md` を通読すること**（禁忌一覧が §3 にある）。

### Phase 4: 公開とサーバーレスバックエンド

- Lambda 側実装（`lambda/handler.ts`）・フロント側送信コード（`src/systems/Api.js`, `GameUI.jsx` のクリア画面/Top10 表示）は**実装済み**。
- しかし **AWS 側の実体（API Gateway / Lambda デプロイ / DynamoDB テーブル）が未構築**のため事実上未接続。`VITE_API_BASE` 未設定時は `Api.js` が no-op で動き、フロントは「オフラインモード」表示になる。
- Milestone 4.1（S3+CloudFront 配信）・4.3（手動 AWS 構築）は未着手。
- 手順は `docs/infra/INFRA.md` に整理済み（2026-09-08 新設。構成図・S3+CloudFront・Lambda+API Gateway+DynamoDB・IAM 最小権限・env 連携の手順書。実施はこれから）。

---

## §4 既知のブロッカー・環境制約

- **ローカル環境の Node.js が v14.18.0 のまま**（`node --version` で確認済み、2026-08-26 時点）。プロジェクトは `>=18`、実用上は Node 20.19.x LTS / 22.x LTS が必要（`ISSUES.md` #14）。このため `npm run lint` / `npm run build` がローカルで実行できない。
  - 対応: **ユーザ側で Node のバージョンアップが必要**（nvm-windows 等）。Claude からは実行不可。
- その他の未解決 Issue（Low〜Nice-to-have、動作への影響は小さい）: `ISSUES.md` #8（lint 警告の未確認）、#9（E キー競合の注意点）、#11（テストカバレッジ拡大）、#12（`.vscode/settings.json` の TS/JS 設定混在）。詳細は `docs/ISSUES.md` を参照。

---

## §5 セッションログ

作業セッションの区切りごとに 1 行追記する（`PS1_REDESIGN.md §4` と同じ運用）。

| 日付 | 内容 |
|---|---|
| 2026-06-08 | PS1 頂点スナップ／アフィンテクスチャ／ディザを試行→いずれもユーザ却下、方針を再設計（`PS1_REDESIGN.md` 新設） |
| 2026-06-09 | PS1_REDESIGN Step 1 の目視確認 OK・`develop` へマージ（`b9f68b4`）。fog 値 `(8, 32)` を先行確定。直後にドキュメント間の齟齬を修正（`5e5f302`） |
| 2026-08-26 | `docs/` の重複記述を整理し、本 `HANDOFF.md` を新設。**この時点でコード上の進捗に変化はなし**（直近のコード変更は 2026-06-09 の `b9f68b4` のまま、Node バージョンも v14.18.0 のまま） |
| 2026-09-08 | `docs/実績情報_取得マップ_確定版.md` を追加し、Phase 6（可観測性）の位置づけを `PROJECT.md`/`CAREER.md` に反映。続けて `docs/infra/INFRA.md`（Phase 4 手動構築手順書）を新設し、`docs/` 全体の重複・陳腐化を整理: `ARCHITECTURE.md`/`CLAUDE.md` に残っていた「フロント送信コード未実装」という stale な記述（実際は実装済み）を3箇所修正、`ISSUES.md` #4（頂点スナップ実装）に後の却下・削除を反映した訂正を追記、`CONVENTIONS.md` §8 の commit ポリシーを `CLAUDE.md` の現行方針と整合させて修正、ルート `README.md` の fog/dpr/松明の数値誤りと Phase2-4 の記載漏れを修正、`CAREER.md`/`SAA_ROADMAP.md` 間で重複していたポートフォリオ・チェックリストを `CAREER.md` 側に一本化。**この時点でコード上の進捗に変化はなし（ドキュメントのみ）** |

---

## §6 ドキュメント地図

一覧と各ファイルの責務は `docs/README.md` を参照。よく使うものだけ再掲:

| 目的 | ファイル |
|---|---|
| PS1 表現に触る前に必読 | `docs/PS1_REDESIGN.md` |
| 既知のバグ・修正待ち | `docs/ISSUES.md` |
| Phase 別の実装ロードマップ詳細 | `docs/roadmap/PROJECT.md` |
| アーキテクチャ・状態管理の詳細 | `docs/ARCHITECTURE.md` |
| コーディング規約 | `docs/CONVENTIONS.md` |
| 次に何をやるかの候補提示 | `/next-task` skill |
